import * as actions from '../constants/ActionTypes';

const initialState = {
  markers: []
}

const map = (state = initialState, action) => {
  switch (action.type) {

    default:
      return state;
  }
};

export default map;
